const Button2 = ({ buttonTitle, buttonColor }) => {
  return (
    <div
    onClick={onClick}
      className="my-button"
      style={{
        backgroundColor: buttonColor,
      }}>{buttonTitle}</div>
  
      );

    };

export default Button2;
